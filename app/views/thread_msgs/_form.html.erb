<%= form_with model: [@post, ThreadMsg.new], local: true do |form| %>
  <div class="field">
    <%= form.label :content, "Répondre au message" %>
    <%= form.text_area :content %>
  </div>
  <div class="actions">
    <%= form.submit "Répondre" %>
  </div>
<% end %>
